name: Build and deploy to Docker Hub
run-name: Build and Push ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}

on:
  # This workflow will ONLY run when a tag is pushed.
  # Pull requests are handled by a separate, simpler workflow if needed.
  workflow_dispatch:
    inputs:
      version:
        description: "Docker tag / Release version (e.g. v1.2.3)"
        required: true    

jobs:
  build-and-push: # Renamed for clarity
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image to Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.aws
          push: true
          # This correctly uses the Git tag (e.g., "v1.2.0") as the Docker tag
          tags: ${{ secrets.DOCKER_USERNAME }}/${{ github.event.repository.name }}:${{ github.ref_name || inputs.version }}